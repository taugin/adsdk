<resources>
    <declare-styleable name="ScanningAnimatorView">
        <attr name="bgImage" format="integer" />
        <attr name="fgImage" format="integer" />
        <attr name="saLineWidth" format="dimension" />
        <attr name="saLineColor" format="color" />
        <attr name="coverColor" format="color" />
    </declare-styleable>

    <style name="AppTheme" parent="android:Theme.DeviceDefault.NoActionBar">
        <!--        <item name="android:windowFullscreen">true</item>-->
        <!--        <item name="android:statusBarColor">@android:color/white</item>-->
    </style>

    <style name="MainActivity_Theme" parent="android:Theme.DeviceDefault.Light">
        <!--        <item name="android:windowFullscreen">true</item>-->
        <!--        <item name="android:statusBarColor">@android:color/white</item>-->
    </style>

    <style name="ButtonStyle">
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:layout_marginBottom">4dp</item>
        <item name="android:layout_marginStart">2dp</item>
        <item name="android:layout_marginEnd">2dp</item>
    </style>
</resources>
